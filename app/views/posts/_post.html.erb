<% div_for post do%>
  <h2><%= link_to_unless_current h(post.title), post%></h2>
  
  <div id="<%= "#{dom_id(post)}_body" %>" data-edit-path="<%= edit_post_path(post) %>">
	<%= simple_format (post.body) %>
	</div>
	
<% end %>


